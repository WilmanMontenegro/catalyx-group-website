---
import { techceleraSocialUrls } from '../data/social';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Consultoría de software y desarrollo a medida para empresas. Diseñamos soluciones únicas para operar mejor, crecer más rápido y sin límites.' } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL("/images/hero-poster.jpg", Astro.site);
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:locale" content="es_CO" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Favicon -->
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

    <!-- Schema.org -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "TechCelera",
      "url": canonicalURL,
      "logo": ogImage,
      "description": description,
      "areaServed": "CO",
      "serviceType": ["Consultoría de software", "Desarrollo a medida", "Implementación de soluciones tecnológicas"],
      "sameAs": techceleraSocialUrls
    })} />

    <!-- Theme init (evitar FOUC) -->
    <script is:inline>
      (() => {
        const savedTheme = localStorage.getItem('theme-mode');
        const isDark = savedTheme ? savedTheme === 'dark' : true;
        document.documentElement.classList.toggle('theme-dark', isDark);
      })();
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
